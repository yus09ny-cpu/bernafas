Placeholder — no backend routes here yet. Bernafas calls the existing Audit
Jiwa backend via `VITE_API_BASE_URL` (see `src/config/env.ts`); this folder
exists only in case a thin proxy/BFF route is ever needed on the
`bernafas.my` Vercel project itself.
